{
  "name": "collab-website",
  "version": "1.0.0",
  "description": "Proyecto colaborativo de sitio web con Node.js + include recursivo",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "start": "npm run build && npx http-server dist --yes -c-1"
  },
  "dependencies": {
    "fs-extra": "^11.1.1",
    "http-server": "^14.1.1"
  },
  "engines": {
    "node": ">=14"
  },
  "license": "MIT"
}
